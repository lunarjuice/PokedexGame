public class SquidSqitch : IMessageSender
{
    public bool ConnectToService()
    {
        // connect to squidswitch
        return true;
    }

    public bool SendMessage()
    {
        // create the message
        // send the message
        return true;
    }
}